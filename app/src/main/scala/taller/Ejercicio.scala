package taller

class Ejercicio {

  /** Las ternas (a, b, c) con a <= b <= c <= n que cumplen a*a + b*b == c*c,
    * en orden ascendente por a y luego por b.
    *
    * Tal como está devuelve la lista vacía y las pruebas quedan en rojo.
    */
  def ejercicio(n: Int): List[(Int, Int, Int)] = {
    List() // Completar con una expresión for
  }
}
